# countries-similarity-clustering
A framework for collecting, preprocessing, comparing, differencing, and clustering Wikipedia country infoboxes. Uses tree edit distance for semi-structured similarity, generates diffs and patches, and groups countries via configurable clustering algorithms.
